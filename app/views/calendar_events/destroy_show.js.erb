$("#empty-modal-body-container").empty();
$("#empty-modal-body-container").html("<%= escape_javascript(render partial: 'event_delete', locals: { event: @event }) %>");
$("#modal-window-empty").modal("show");
