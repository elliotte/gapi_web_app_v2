$("#empty-modal-body-container").empty();
$("#empty-modal-body-container").html("<%= escape_javascript(render partial: 'event_show', locals: { event: @event }) %>");
$("#modal-window-empty").modal("show");