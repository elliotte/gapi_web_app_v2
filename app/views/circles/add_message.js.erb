$("#modal-body-circle-add-message").html("Success, message added.");

message = {
	text: '<%= @message.text %>',
	id: '<%= @message.id %>',
	added_by: '<%= @message.added_by %>'
}

$("#team-messages-container").prepend(uiHelper.teamMessagesHtml(message));

 Webflow.require("slider").redraw();

 