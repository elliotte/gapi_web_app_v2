$('.files-display-modal').modal('hide');

$("#empty-modal-body-container").empty();
$("#empty-modal-body-container").html("<%= escape_javascript(render partial: 'file_copy', locals: { file: @file }) %>");
$("#modal-window-empty").modal("show");

$('#copy_button').click(function(){
	$('#copy_file').submit();
});

