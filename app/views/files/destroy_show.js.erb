$('.files-display-modal').modal('hide');

$("#empty-modal-body-container").empty();
$("#empty-modal-body-container").html("<%= escape_javascript(render partial: 'file_delete', locals: { file: @file }) %>");
$("#modal-window-empty").modal("show");