$('.files-display-modal').modal('hide');
$("#empty-modal-body-container").empty();
$("#empty-modal-body-container").html("<%= escape_javascript(render partial: 'file_comments', locals: { comments: @comments }) %>");
$("#modal-window-empty").modal("show");